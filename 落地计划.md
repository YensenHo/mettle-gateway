# 机器支付网关 · 落地计划（MVP 版）

> 方向：本土化"HTTP 402 机器支付网关"（人民币结算，服务 AI Agent / MCP 工具按次付费）
> 原则：**计量层不碰钱** —— 网关做「计量 + 协议 + 对账」，钱走微信/支付宝直连工具开发者，规避支付牌照
> 日期：2026-08-13 | Hermes 起草，Codex 技术把关

---

## 0. 一句话定位

> **给 AI Agent / MCP 工具开发者一个"按调用次数收费"的计费网关 —— 中国版 x402，用人民币结算，网关不托管资金。**

---

## 1. 为什么这个方向成立（证据链）

| 证据 | 来源 |
|------|------|
| 海外 x402 近 30 天真实交易 $2424 万、7541 万笔 | x402.org 面板 |
| Cloudflare 已铺好 Agentic Internet 基础设施 | 官方博客 |
| 海外已出现法币结算 facilitator（Floe：银行卡/Apple Pay，无需 crypto） | x402 README |
| 国内稳定币受限，但 API 计费/代理站生态全球最成熟 | One-API/new-api |
| 国内 Agent 平台（扣子/百度/通义/豆包）工具开发者缺"按次收费"机制 | 待 Codex 蓝海报告确认 |

**核心机会：海外验证了"请求即支付"模式，国内还没有人民币版。**

---

## 2. 架构：计量层不碰钱（关键设计）

```
                    ┌─────────────────────────────┐
                    │        机器支付网关 (本产品)     │
                    │  · 计量（统计工具被调用次数）     │
 Agent / MCP 客户端  │  · 402 协议握手（报价/放行）     │
      │             │  · 对账 / 出账单 / 额度管理      │
      │ 调用工具      │  · 【不碰钱】                   │
      ▼             └──────────────┬──────────────┘
  ┌─────────┐                      │ 计量结果 + 账单
  │ 工具开发者 │◄──── 月结账单 ──────┘
  │ (MCP工具) │
  └────┬────┘
       │ 钱直接走微信/支付宝（工具开发者自己的商户号）
       ▼
  ┌─────────────┐
  │ 微信/支付宝    │   ← 网关不经过、不托管
  └─────────────┘
```

### 两个阶段

| 阶段 | 模式 | 碰钱? | 牌照 | 何时做 |
|------|------|------|------|--------|
| **MVP** | 月结后付费（类似 Stripe metered billing） | ❌ | 无需 | 现在 |
| 进阶 | 实时微支付（免密代扣 / 预充值） | ✅ | 特约商户/存管 | 有量后 |

### 网关收费方式（MVP）

- 不抽成（不碰钱，也抽不了）
- 向**工具开发者**收 **SaaS 订阅费 / 计量服务费**（如 ¥99/月 基础版，¥499/月 专业版按量）
- 类比：Stripe 收 2.9% 是抽成，但"计量 SaaS"（如 metering 中间件）收的是订阅费

---

## 3. 核心功能（MVP 范围）

### 开发者侧
1. 注册工具/MCP 端点，设置计费规则（按次 / 按 token / 按月）
2. 绑定自己的微信/支付宝收款方式
3. 实时看调用量、收入、账单
4. 生成接入 SDK（一行代码接入计费）

### Agent/用户侧
1. 预充值"额度"（虚拟额度，非真钱 —— 见下）
2. 调用工具时，网关校验额度 → 放行或 402 拒
3. 月度出账单，走微信/支付宝支付

> **关键澄清**：MVP 阶段"额度"是**虚拟计费单位**（网关自己记账），不是真钱托管。用户的真钱在月结时直接付给工具开发者。网关只是"记账的"，所以不碰钱、无牌照问题。

---

## 4. 复用 Prometheus 资产（不重写，只改造）

| Prometheus 旧组件 | 新网关角色 |
|-------------------|-----------|
| 撮合引擎 | 计费规则匹配（哪个工具按什么价） |
| 担保交易 7 态状态机 | 账单生命周期（待结算→已出账→已支付→已结算） |
| 余额验证（DeepSeek） | 额度校验（调用前查虚拟额度） |
| Agent 管理 | 工具开发者注册 |
| 单 HTML + FastAPI + SQLite | 沿用（用户偏好的 MVP 形态） |

---

## 5. 里程碑（业余可推进）

| 阶段 | 时间 | 交付 | 说明 |
|------|------|------|------|
| **M1** | 第 1-2 周 | 网关核心：工具注册 + 计费规则 + 额度管理 + 402 握手 | FastAPI + SQLite + 单 HTML |
| **M2** | 第 3-4 周 | 计量引擎 + 月结账单 + 对账 | 接入 SDK（Python 一行） |
| **M3** | 第 5-6 周 | 找 3-5 个 MCP 工具开发者试用 | 验证"按次收费"真实需求 |
| **M4** | 第 7-8 周 | 上线 + 开源 + Build in public | 建立早期社区 |

---

## 6. 风险与对策

| 风险 | 对策 |
|------|------|
| 支付牌照（若未来碰钱） | MVP 阶段不碰钱，纯计量 SaaS |
| 微信/支付宝"自动扣款"受限 | MVP 用月结后付费，不做实时微支付 |
| 巨头（阿里/腾讯云）抄 | 先做开源 + 社区，建"标准 + 数据"层（参照之前 15 轮辩论结论） |
| 无需求 | M3 阶段先验证，不妥就全删 |

---

## 7. 蓝海确认 + 竞品定位（Codex 排查定稿）

### ✅ 蓝海确认（针对"人民币结算轨的机器支付网关"这一切片）

一手证据链（2026-08-13）：
1. 海外 x402 生态成熟但**只用 USDC/稳定币 + 西方银行轨**，GitHub 搜 `x402 中国` = **空结果**
2. 国内 Agent 平台工具变现 = 平台计费分成/广告/打赏，**无"请求即支付"的 MCP 按次付费**（扣子受邀+企业版门槛、百度广告分成、元器/百炼/豆包/Kimi 无机制）
3. 国内 API 计费网关（one-api 系，⭐36k）= 账户+充值+令牌额度，**无机器支付、无 x402**
4. 国内 MCP 市场（ModelScope/智谱/百炼）= 免费分发，**工具开发者无自助变现通道**
5. 国内直接竞品 = **仅有 0-star 个人死项目**，无成熟创业公司
6. e-CNY 可编程支付 = 可用的底层基座（智能合约/条件支付/物联网硬钱包），但**无 M2M 开发者 API**

### ⚠️ 必须警惕的红海（避免误判"纯蓝海"）

| 风险 | 说明 |
|------|------|
| 大方向不蓝 | Agent 工具收费已被字节/百度/腾讯/阿里用"平台计费分成"圈地 |
| 合规/牌照 | 人民币清结算涉及支付牌照、二清、反洗钱（最大壁垒） |
| 海外 fiat 反扑 | x402/Floe/Moltpe 已声明"支持 fiat"，可能接通跨境人民币 |
| 大厂随时入场 | 字节/微信支付/支付宝掌握结算轨 |

### 🎯 最终定位（差异化）

> **不做"通用 Agent 工具市场"（红海），做"人民币结算轨的机器支付/计费基础设施层"** —— 把 x402/HTTP 402 语义 + MCP 按次计费 + 微信/支付宝/数字人民币结算轨，封装成**开发者自助接入的网关**。

切的是：**海外 x402 没做的人民币轨** × **国内平台没做的"无账户请求即支付"** 之间的真空带。

---

## 8. 待办（下一步）

- [x] 蓝海确认
- [ ] 产品命名
- [ ] 技术栈最终确定 + M1 原型开发
- [ ] 找 3-5 个 MCP 工具开发者验证需求

---

*计划 v1.0 定稿，蓝海已确认。*
